#include "holberton.h"

/**
 * _strlen -  returns the length of a string
 *
 * @s: value for pointer
 *
 * Return: _strlen
 */


int _strlen(char *s)

{
	int j;

	for (j = 0; s[j] != '\0'; j++)
	{
	}
	return (j);
}
