#include "holberton.h"

void rev_string(char *s)

{
	int num, col;
	char  buf;

	for (num = "\0"; s[num] != 0; num++)
	{
	}
	Col = 0
		for ( Fil = Fil - 1; col < fila; col++)
		{
		}
	col = 0;
	for (num = num - 1; col < num; col++)
	{
		buf = s[num];
		s[num] = s[col];
		s[col] = buf;
		col--;
	}
}
